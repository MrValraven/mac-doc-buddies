# M11 — decisions taken on your behalf

Generated 2026-08-25 from the execution ledger, which is scratch and gets deleted.
Every line here is a call I made without asking, with what it costs if it was wrong.
Read it and reverse anything you disagree with — that is what it is for.

The plan is at [docs/superpowers/plans/2026-08-25-m11-two-cats.md](../plans/2026-08-25-m11-two-cats.md)
and the spec at [SPEC.md](../../../SPEC.md) milestone M11.

---

- **R1** — `PetProfile` gets a hand-written `init(from:)` defaulting every missing key (`name`/`userName` to nil, `color` to `CatPalette.default.id`), mirroring `PetConfig.init(from:)`. — A partial `pets` entry must not throw away the whole config file; Global Constraints say bad values are clamped and logged, never fatal. — If wrong: a typo'd coat key silently becomes orange instead of failing loudly, which is the spec's stated policy anyway. Low cost.
- **R2** — the bubble method is `showBubble(_:)`, and Task 6 requires dropping its `private`. Task 3 calls it unqualified from inside `PetInteraction`; Task 6 calls `pet.interaction.showBubble(...)`. — Two code paths that present a bubble would drift, exactly as the spec says about the Accessibility prompt. — If wrong: one method widens from private to internal inside the app target. Negligible.
- **R3** — tests call `Harness.bail(...)` bare, not `return Harness.bail(...)`. — Matches all 9 existing call sites; `Never` in return position is legal but unlike the surrounding code. — If wrong: nothing; both compile.
- **R4** — only the menu-building loop (`PetInteraction.swift:138`) filters `.birthday`. The other four `PetPrompt.allCases` sites correctly include it: the launch log's prompt count, `--interaction-test`'s per-prompt bubble check, and two `PhrasebookTests` loops that assert every pool is well-formed. — The birthday pool should be covered by those checks; suppressing it everywhere would leave the one pool that matters on the day untested. — If wrong: the launch log says 5 prompts instead of 4, and one extra case runs in the interaction test. Benign.
- **R5** — the dedication and the birthday swap must be suppressed under any self-test. `PetInteraction` gains `var isSelfTest = false`, set from `LaunchOptions.isSelfTest` when the pet is built; `say(_:)` skips both the dedication branch and the `.hello`→`.birthday` swap when it is true. — Otherwise running `--interaction-test` on the morning of the birthday consumes the once-a-day dedication and writes `state.json`, so Philippine's first real click gets an ordinary hello. This is the one pre-flight finding that damages the gift itself. — If wrong: the interaction test never exercises the dedication path, which is covered by `MeetingTests` and by hand at Task 3 Step 10 instead. Low cost.
- **R6** — work proceeds on a branch `m11-two-cats` in the primary working copy, not a git worktree. — Every verification step in this plan runs the real app against state a worktree does not isolate: `~/Library/Application Support/DockPet/`, and an Accessibility grant keyed to the code signature (SPEC §8.6). The repo is created by Task 0 and has no other work to conflict with; the baseline commit on the default branch stays as the escape hatch. — If wrong: the branch is trivially rebased or merged. Negligible.
- **Task 0 reviewed by direct verification rather than a dispatched task reviewer.** — The task produces no application code: one `.gitignore` and a baseline commit of pre-existing files. A review seat would have nothing to review, and I ran the same checks a reviewer would. — If wrong: negligible; the baseline is verified by build, test suite, `git ls-files` and `git status`.
- **R7** — Task 1's probe as written cannot answer its own question and must be replaced. — `SMAppService.mainApp` resolves against the *running process's main bundle*; a loose `swiftc` binary has none, so the plan's `/tmp/loginprobe` returns `.notFound` unconditionally and would wrongly select the launchd fallback. The probe instead implements the `SMAppService` branch, runs `./bundle.sh`, and reads the `[config] launch at login:` line that Step 7 already prints from inside the real bundle. — If wrong: the implementer reports the observed status and I re-rule; no code is thrown away either way, since Step 7's log line is in the plan regardless.
- **Task 1 finding 1 (plan-mandated)** — the reviewer is right and the plan is wrong. The brief's Step 7 specifies the `applyConfig` addition with no self-test guard, so `--interaction-test` calls `applyConfig` with a synthetic `PetConfig` that defaults `launchAtLogin` to true, really registering the user's login item and unregistering it again — and leaving it registered if the test aborts between the two. That directly violates the invariant the task's own new comment states ("a test run must not rewrite the user's login item as a side effect of checking something else"). Fix it: guard the `applyConfig` call site with `options.isSelfTest`, the same way the launch-time call already is. — If wrong: a self-test would no longer exercise the login-item path, which is covered by Step 9's hand verification instead. Low cost. The plan is authoritative only where the spec does not contradict it, and here it contradicts itself.
- **Task 1 finding 2** — fix the comment, not the code. `LoginItem.swift`'s doc comment describes a launchd fallback that R7 correctly caused not to be written. R7 stands; the comment is stale template text and must say plainly that SMAppService was verified against the signed bundle and no fallback exists. — If wrong: nothing; it is a comment. But a maintainer debugging a registration failure would hunt for a branch that never existed.
- **R8** — all work moves to `main`; branch `m11-two-cats` deleted. — Direct user instruction ("carry on, but always work on the main or master branch"), which overrides the subagent-driven-development skill's isolated-branch requirement and supplies the explicit consent that skill asks for. Saved to memory as `work-on-main-branch`. Moved with `git checkout -B main`, NOT a checkout, so the parallel session's 8 uncommitted files were never touched. — If wrong: history is linear and trivially re-branched.
- **R9** — carry on despite the collision with docpets-78, per direct user instruction. Mitigations, binding on every remaining dispatch: (1) commit named files only, never `git add -A`; (2) never run `git checkout --`, `git stash`, `git reset` or any tree-restoring command on ANY file — the other session's work is uncommitted and unrecoverable; (3) when a shared file (AppDelegate.swift, Config.swift, ConfigTests.swift) holds both my hunks and theirs, commit only my own hunks via a hand-built patch, as Task 1's implementer did. — If wrong: their olive-coat work is destroyed with no way back. This is the one risk in the run with no undo, so the mitigation is mandatory, not advisory.
- **R10** — accepted the implementer's deviation from the brief's literal `AXIsProcessTrusted()` in favour of `DockTiles.isTrusted`. — I instructed it to match the codebase's existing idiom over the brief's stale snippet, and every other call site in the repo uses the wrapper; two spellings of the same check are how drift starts. — If wrong: the wrapper could mean something subtly different from the raw call, so the reviewer was asked to verify its definition as a named risk. Low cost.
- **Task 2 finding 1 (plan-mandated)** — the reviewer is right and my plan's window code is wrong. A `wrappingLabelWithString:` label inside a `.leading`-aligned NSStackView has no trailing constraint and no `preferredMaxLayoutWidth`, so it can lay out at its natural near-single-line width and be clipped by a non-resizable 420pt window; and the hardcoded 190pt height leaves too little budget once the body wraps to 3-4 lines. Fix: constrain the body's width AND let the window size itself to the stack's fitting height instead of hardcoding 190. — Guessing a second fixed height would repeat the same mistake with a different number. — If wrong: the window is a little taller or shorter than intended, which is visible and trivially adjusted. Cost of NOT fixing: the one message the task exists to deliver renders clipped on the first screen Philippine ever sees, and no test in this repo would catch it.
- **Task 2 finding 2** — fix it, though I would have rated it Minor. `onboarding` is never set back to nil, so a hidden NSWindow is retained for the app's life and every 500 ms poll dereferences it. Guarded and cheap, so not a correctness bug — but the fix is two lines and I am not going to spend a review round arguing severity down. — If wrong: nothing. — Recording the disagreement rather than silently downgrading it.
- **Task 2 minor (keyEquivalent)** — FOLDED INTO THE FIX rather than deferred. `orderFrontRegardless()` without making the window key means the default button's Return key may not fire until the user clicks into the window. SPEC §3's never-make-key rule constrains the *pet* window only; this is an ordinary titled window and may be key. One-line change on the highest-stakes surface in the project. — If wrong: the window takes focus on launch, which is what a permission prompt should do anyway.
- **R11** — the plan's task order is re-cut along an AppKit boundary rather than followed literally, to parallelise. Tasks 3 and 6 are each half pure `DockPetCore` logic (Occasion, the birthday pool, MeetingLine/meetingPairs, MeetingCoordinator, Walker.reverse, and all their tests) and half AppKit wiring (StateStore, PetInteraction, AppDelegate). The pure halves share no file with anything now in flight, so they run as one combined agent (P) immediately, concurrent with Task 2's fix round. — The plan's sequence was chosen for reviewability, not because these have a dependency; splitting on the DockPetCore/AppKit line is the same split the spec already enforces by linkage (§5: DockPetCore links CoreGraphics only). — If wrong: the pure work lands ahead of its consumers and sits unused until the wiring tasks arrive, which costs nothing. The task briefs and review gates are unchanged; only the batching is.
- **R12** — the `onboarding = nil` clear is gated on `isFinished && !isVisible`, not `isFinished` alone. — The implementer chose this and the re-reviewer verified the timeline: `isFinished` flips at T=0 but the window stays visible until the delayed `orderOut` fires at T≈2.5s; clearing on `isFinished` alone would drop the last strong reference mid-message. — If wrong: the window reference lingers one extra poll tick. Negligible either way, and the stricter gate is correct.
- **P deviation 1 ACCEPTED** — the plan's leap-day test used `date(2026, 2, 29)`, and 2026 is not a leap year, so the test asserted against a date that does not exist. Changed to 2024. — My plan was simply wrong; a Gregorian calendar would either roll the date or return nil, and the test would have been meaningless either way. — If wrong: nothing; 2024 is a leap year and the assertion now tests what it claims to.
- **P deviation 2 ACCEPTED** — the plan's cooldown test called `advance(by: 59)` in a single call while the plan ALSO required `advance` to clamp to `BehaviorMachine.maximumStep` (1.0s). Those two requirements contradict: with clamping, one call advances one second and the test fails. Changed to a loop of small steps, mirroring how `BehaviorTests` already handles the same tension for `BehaviorMachine`. — Two of my own requirements were mutually exclusive and the agent picked the one the spec cares about (bounded steps, so a stalled process cannot burn the cooldown in one tick) over the one that was merely convenient to write. — If wrong: the cooldown is tested through many small steps rather than one large one, which is closer to how it actually runs. The reviewer was asked to verify the clamping really exists, since this ruling depends on it.
- **P review finding 1 (plan-mandated)** — FIX IT. My own test at MeetingTests.swift:746-748 is vacuous: its third disjunct `!opener.contains(nameSlot)` was already asserted true two lines earlier and is true after ANY successful render, so the whole `||` chain passes regardless of which name went into which line. It would pass identically if `meet()` swapped openerName and replierName. The reviewer verified by inspection that the implementation is correct — but the behaviour is load-bearing and subtle (each line must address the OTHER cat; a swap reads perfectly fine in isolation) and it is the actual charm of the feature. A test that cannot fail on that is worth nothing. — If wrong: a slightly stricter test. Cost of not fixing: a future edit silently swaps the two names and nothing catches it.
- **P review minor 2** — FOLDED INTO THE FIX. `Occasion.parse`'s range check is the one item on the review checklist the suite never actually confirms; `"13-01"` and `"02-30"` are two lines of test. Note `"02-30"` IS accepted by design (parse range-checks 1...31 generically, not per-month) — pin that documented behaviour rather than change it.
- **P review minor 1** — DEFERRED to final review. The reroll-without-repeat logic is duplicated between `Phrasebook.reply` and `MeetingCoordinator.meet`. Six lines, and the duplication matches the codebase's existing single-file locality. Extracting a shared helper now would touch a file Agent B is not in but a later agent might be. — If wrong: the two copies drift. Low risk; both are short and adjacent in purpose.
- **B concern 3 ACCEPTED** — B created a fifth file, `PetSelfTests.swift` (352 lines), outside its brief's file table. — A straight extraction left AppDelegate at 1413 lines, LARGER than the 1341 it started at, which defeats a stated goal of the step; moving the three pet-facing self-tests out brought it to 1080. It deliberately left `runSettingsTest` in place because that is exactly where the parallel session is editing — a genuinely thoughtful call. — If wrong: one more file in Sources/DockPet/, easily folded back. Flagged to the reviewer as unbriefed scope rather than hidden.
- **B concern 1 DEFERRED TO AGENT C** — the dedication branch returns early, so a walking cat keeps walking under its own dedication, and `lastReply` is never set so the one sentence that matters is invisible in the verbose log and to `--interaction-test`. B was right not to improvise: the brief said verbatim, and I would rather it flag than freelance. C owns `PetInteraction.swift` and fixes it there. — If wrong: two lines move between agents. — This is a real defect in MY plan's Step 8, not in B's work.
- **B concern 2 EXPECTED, NOT A DEFECT** — `interactionForcePetState` still forces `primaryPet`. Correct with one cat, wrong with two, and Agent C's brief already carries the fix as its most-likely-bug warning. B additionally made `--interaction-test` assert on `pet.behavior.state`, so this will fail loudly when the second cat lands rather than passing silently. That is better than fixing it early.
- **B review found THREE consequences of my plan's dedication early-return that I had missed, all going to Agent C.** — (a) On the birthday itself the `.hello -> .birthday` swap runs FIRST and is then discarded by the dedication's early return, so on the one morning both features fire the user must click twice to ever see a birthday line; the ordering is backwards. (b) The dedication swallows `.nap` outright — the first click of the day on "Take a nap" shows the dedication and the cat does NOT sleep, because `forcedState` is never applied below the return. The user's chosen action is dropped, not deferred. (c) `lastGreetedDay` is stamped BEFORE `showBubble`, which returns silently if the delegate is nil — unreachable today, but it makes "the day is consumed with nothing shown" a one-line change away. — These are defects in MY plan's Step 8, not in any agent's work. (b) is the worst: silently dropping a user's action is not a cosmetic bug, and it happens on first click of every day. — If wrong: the dedication fires on a later click than intended, which is visible and adjustable.
- **B review Important 1 -> AGENT C, and partly already in its brief. Per-pet coat is stored and reported at three sites (launch log, verbose log, --render-test) but the sprite set is loaded once globally, so all three will confidently report a coat the cat is not wearing the moment pet 1 differs. C's brief already requires one recoloured SpriteSet per distinct coat, which fixes the cause; ADDED to C: make --render-test ASSERT the relationship rather than print it, since it currently prints the value and excludes it from the ok boolean.
- **B review Important 2 -> AGENT C. `Pet` owns an NSWindow and a global NSEvent monitor with no teardown path. Harmless while the array is built once, but C makes `pets` come from config and rebuilds on Settings change; an orphaned ordered-in window leaves a cat on the Dock forever with a live mouse monitor behind it** — a leak that looks exactly like a rendering bug. Five lines now while the invariant is still trivially true.
- **B review Important 3 -> DEFERRED to a follow-up agent, not dropped. The dedication, the birthday swap and all of StateStore have ZERO automated coverage in both directions: `isSelfTest` is true for all five modes, and GeometryTests links DockPetCore only, so the suite cannot import PetInteraction or StateStore at all. Nobody has verified the POSITIVE** — that the dedication is ever said — on the feature the milestone exists to deliver. C is already carrying a lot and this needs a new launch mode plus a temp-dir StateStore; doing it after C keeps the two out of the same files. — If wrong and it never gets done: the centrepiece ships verified only by hand. Tracked as MUST-DO, not deferred-minor.
- **C concern 3 is WRONG** — verified by reading the code myself rather than accepting it. `MeetingCoordinator.advance` clamps with `BehaviorMachine.maximumStep`, which is 1.0 (Behavior.swift:154), NOT `Walker.maximumStep` which is 0.25 (Behavior.swift:42). C conflated the two. With the poll's dt of ~0.5s, `min(0.5, 1.0) = 0.5`, so the cooldown accumulates in real time and 60s means 60s. No doubling, no bug, no action. — Agent P's reviewer had already verified this same line and value independently; two agents agreeing against one is not why I checked, the code is.
- **C's dedication-vs-birthday ordering ACCEPTED** — it decides the dedication BEFORE the `.hello -> .birthday` swap, so on the birthday the swap never happens and the greeting is still unspent on the next click. That is better than what I asked for: I said "the greeting must not be lost", and deciding first means nothing is ever discarded rather than discarded-and-restored. Probe-verified by C that the nap still fires on the dedication click. — If wrong: on the birthday the first click is the dedication and the second is the birthday line, which is the intended feel anyway.
- **C review Critical 1** — FIX. `SettingsWindow.currentValues()` takes `secondColorPopup.selectedItem ?? Self.coatUnlike(coat)`, but `fillCoats` always ends with `selectItem(at: index ?? 0)`, so `selectedItem` is never nil and the fallback is unreachable dead code. The new cat therefore always gets `CatPalette.all[0]`, which at HEAD IS the default coat — tick "A second cat" on a default config and you get two identical orange cats, exactly the outcome the brief singled out. Worse: this diff's OWN new assertion (`pets[1].color != soloCoat`) fails deterministically on the committed tree, and passed in the implementer's run ONLY because its build included the parallel session's uncommitted olive-first catalogue where `all[0] != "orange"`. — The reviewer demonstrated this from initial values without needing a screen. — If wrong: nothing; `coatUnlike` already exists and is correct, it is simply never called.
- **C review Critical 2** — FIX, HIGHEST PRIORITY IN THE PROJECT. `currentValues()` omits `birthday` and `dedication`, which default to nil; `applyConfig` assigns `config = newConfig` wholesale and `ConfigStore.write` replaces the file. So opening Settings and touching ANY control silently erases the birthday and the dedication from disk, with no way back short of hand-editing JSON. Philippine nudges the speed slider and the entire gift layer this milestone exists for is gone. — The shape predates the diff, but the function was rewritten here in a file this task owns, and the author applied exactly the right reasoning one field over (the `loadedPets` comment explains at length why a cat's name must be carried through). — If wrong: nothing. This is the single most damaging bug found in the run.
- **C review Important 3** — FIX. Both pets construct `Walker(speed:)` with distance 0 and no offset, so on a real Dock they launch superimposed, and on the FIRST located poll `haveMet` (`a.intersects(b) || a == b`) is true for identical frames while `sinceLastMeeting` starts at `cooldown` — so two stacked cats immediately spend the meeting on themselves, burning the 60s cooldown on something nobody can read as a meeting, and only separate once their behaviour machines diverge. `rebuildPets` reproduces it on demand and additionally teleports the existing cat back to the left end. — If wrong: the cats start spaced out, which is what anyone would expect anyway.
- **C review Important 4** — FIX. `--interaction-test` hand-builds a `PetConfig` with `pets: []`, which `cast(of:)` normalises to one profile; under a two-cat config that triggers `rebuildPets` mid-test, so the captured `primary` is a closed detached pet and the userName check then passes via the nil-pet FALLBACK rather than against a live pet. And `grep userName PetSelfTests.swift` returns exactly one hit: there is NO assertion anywhere that pet 1 greets with pet 1's name. The nap half of the headline two-cat bug is covered per pet; the name half rests entirely on code reading.
- **C review minors 5, 7, 9, 6** — FOLDED INTO THE FIX (one to three lines each, and each is a stated-behaviour mismatch rather than polish): opener is hardcoded `pets[0]` rather than the left-hand cat despite `aIsLeft` being computed right there; the delayed reply re-checks membership but not `isTalking`, so a click during the 1.5s gap gets its bubble stolen by the meeting — the inverse of the precedence rule; coat reload compares SETS of palette ids, so a simultaneous two-coat swap is a no-op and each cat keeps the other's art while every log claims otherwise (latent, and exactly the failure Part 0b exists to prevent, reintroduced one level up); and `rebuildPets` empties `pets` before it knows the sheets will load, so a load failure takes every cat away while the doc comment three lines up claims it cannot.
- **R14** — Task 7 edits README.md ADDITIVELY around the parallel session's uncommitted text rather than rewriting the block, and if a hunk unavoidably carries their text, it is committed WITH their text. — Their edits (the `"color": "olive"` line in the config example and the rewritten `color` bullet) sit in exactly the block my plan's Task 7 rewrites. This is a genuine content conflict, and the user has already ruled once to carry on regardless, so re-asking would re-litigate a settled decision. Committing their text preserves their work in git, which is strictly safer than leaving it uncommitted; they lose nothing either way. Rewriting or reverting their prose would not be safe, and is forbidden in the dispatch. — If wrong: a README commit contains two paragraphs I did not write, attributed to a commit of mine. Trivially amendable, and their work survives.
- **two residuals from the re-review go to the FINAL REVIEW fix wave rather than another round here. (a) `resetToDefaults()` still takes the fragile enumerated shape** — it rebuilds PetConfig.default and explicitly re-copies birthday/dedication, so a third gift-layer field would be silently wiped by Reset. Same class as Critical 2 but far narrower: Reset is a rare explicit action, not every settings change. (b) `startingPlace` has an untested short-strip edge case where near and far ends could coincide, and a newly-added cat's spawn is not checked against a surviving carried cat's position. Outside any real Dock geometry. — Neither reproduces the class of bug its finding targeted, and a sixth round on Agent C costs more than folding two small items into the wave that is coming anyway. — If wrong: (a) bites only if someone adds a fourth gift-layer key AND the user hits Reset; (b) bites only on a Dock narrower than two cats.
- **FINAL C1** — MUST FIX, and it is the single most important finding of the entire run. On a Mac with NO `config.json` — which is exactly the hand-over machine — `ConfigStore.load()` returns `PetConfig.default` WITHOUT calling `.validated()` (ConfigStore.swift:41 and :55). `PetConfig.default`'s `pets:` defaults to `[]`, so `AppDelegate.swift:204`'s `let profiles = config.pets` is empty, no sprite sets load, and the guard at :218 calls `exit(1)`. The app dies silently on first launch: no window, no menu bar item, nothing. The SECOND launch works, because launch #1 wrote the defaults to disk before dying. A non-developer will never discover that by trying again. It also breaks SPEC §1's "bad values are clamped and logged, never fatal" — a hand-edited config.json with a syntax error is now fatal. Nobody caught it because all five self-test modes run on a machine that already has a config.json. — The comment at :204 asserts "validated() guarantees this is never empty"; the premise is false on exactly those two paths. Fix uses `Self.cast(of:)`, which already exists at :1454 for this case. — If wrong: nothing; cast(of:) is the documented normaliser.
- **FINAL I1** — MUST FIX. The onboarding window's only button calls `AXIsProcessTrustedWithOptions([prompt: true])`, which presents its alert ONLY while no TCC record exists. Once the user has answered or dismissed it — including by clicking Deny — the button labelled "Open Accessibility Settings…" is permanently inert, the status line still reads "Waiting for permission…", and the cat never appears. That is precisely the state SPEC 11a says this window exists to prevent. The label also overpromises: nothing in Sources/ opens System Settings. Compounding it, the launch-time requestTrust and the window fire on the same run-loop turn, so a first-run user gets the system TCC alert AND DockPet's window activating over it. Fix: open the pane by URL, and suppress the launch-time prompt when the window is about to show. MenuBarItem's "Grant Accessibility…" has the same dead-button problem.
- **FINAL I2** — MUST FIX. `applyConfig` drops `LoginItem.setEnabled`'s Result (AppDelegate.swift:1249-1251). SPEC 11a requires a registration failure be "logged and clamped to off, never fatal"; neither happens, so a user who ticks the box on a machine that refuses registration gets a ticked box, `launchAtLogin: true` on disk, and no indication it did not take — a plausible outcome on a Mac that does not trust our local signing identity, and exactly what the clamp rule was written for.
- **FINAL I3** — MUST FIX. The meeting shows the opener's bubble and, 1.5s later, the replier's, and nothing dismisses the first. Both bubbles sit at the same y and their pets are 34-64pt apart, while a typical line is ~215pt wide — so for ~1.5s two opaque bubbles with two tails overlap. The milestone's headline feature, never seen by anyone, would read as a rendering glitch. Fix: dismiss the opener's bubble before showing the reply.
- **known item 10 PROMOTED to must-fix, per the reviewer. The meeting is invisible to `--verbose` (`showBubble` never sets `lastReply`, and no `logLocation` fires for the forced sit), so a snapshot mid-exchange prints `talking=true` with no `saying=` and no state line. Since nobody has ever seen the meeting, --verbose is the ENTIRE observation channel. Two lines.
- **FINAL M6 folded into the fix wave** — the onboarding window is `.closable` and its reference is cleared only on `isFinished && !isVisible`, so a user who closes it by hand without granting leaves it retained forever while the poll re-evaluates twice a second. Reachable on the very first run. One line.
- **FINAL M1 folded in if it does not collide** — ConfigTests has NO coverage of `birthday` or `dedication` validation, the two keys the milestone exists to deliver and the two least-tested in the file. Four assertions. Note ConfigTests.swift is one of the parallel session's files, so hunk discipline applies.
- **FINAL M4** — NOT MINE TO FIX, surface to the user. README at 12d499f documents an `olive` coat as the default, but `CatPalette` on this branch has no olive (`all = [orange, grey, black, white, tuxedo, siamese]`, `default = orange`). Anyone following the README gets a "color=olive is out of range, using orange" correction. This is the two change sets colliding in COMMITTED history: the README text came from the parallel session's uncommitted work, preserved per R14. Reverting their prose is not mine to do, and landing their palette is not mine either. Tiago decides: land the olive work, or revert those README lines. — If wrong either way, one README paragraph is stale.
- **Agent D's attribution of the --render-test failures was WRONG, and I verified it rather than accepting it** — the second misattribution to the parallel session in this run. Method: `git archive` of committed HEAD and of the M10 baseline into scratchpad copies (archive reads history only and cannot touch the working tree or their uncommitted work), then built and ran each.
- **the --render-test discrepancy is RECONCILED, and both measurements were correct. `Resources/sprites/` is GITIGNORED** — the sprite art is not versioned. My scratch snapshots had no art, so `swift makesprite.swift` regenerated it from the COMMITTED makesprite.swift, which produces art inconsistent with the committed CatPalette.base -> 14 failures. The real working tree has art regenerated at 17:10 by the parallel session's MODIFIED makesprite.swift, which the same checks pass against -> 0 failures. Neither agent was lying; the variable is which art is on disk, and it is not in git.

---

## Deferred, not forgotten

- Task 0: minor (deferred): the implementer wrote a 70-line `.gitignore` where the brief specified 4 lines. Verified correct — every ignored path is regenerated by a committed generator (`makesprite.swift` writes all four cat sheets, `makeicon.swift` writes the icon, `bundle.sh` runs both when missing), and nothing hand-authored is ignored. Accepted rather than looped on.
- Task 1: minor (deferred): `SettingsWindow.swift:248-260` — `simulate(...)` gained a `launchAtLogin: Bool?` parameter no `--settings-test` assertion exercises. Consistent with the file's existing per-field pattern; dead surface area, not over-engineering. Triage at final review.
- Task 1: minor (deferred): SettingsWindow `simulate(launchAtLogin:)` unexercised.
- Agent P: minor (deferred): reroll-without-repeat duplicated across Phrasebook.reply and MeetingCoordinator.meet.
