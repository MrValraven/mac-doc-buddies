# SDD ledger — plan: docs/superpowers/plans/2026-08-25-m11-two-cats.md

Spec: SPEC.md (milestone M11 + amendments to §1, §6, §8.6). Read and reachable.

## Pre-flight conflict scan

### Cross-task rows (tasks sharing a file or an interface)

| Tasks | Produces / consumes | Finding |
| --- | --- | --- |
| 1, 3, 4, 5 → `DockPetCore/Config.swift` | each appends a defaulted param to `PetConfig.init` and a key to `init(from:)` | Clean. `PetConfig.default` calls `init` with labels, so appending defaulted params is safe in any order. |
| 4 → 5 | T4 creates `PetProfile`; T5 decodes `[PetProfile]` from JSON | **CONFLICT (R1).** Synthesized `Codable` throws on a `pets` entry missing `color`, contradicting Global Constraint "bad values are clamped, never fatal" and `PetConfig`'s own documented partial-config behaviour. |
| 3 → 5 | T3 calls `delegate?.interactionUserName`; T5 turns it into `interactionUserName(for:)` | Clean — plan already documents this at T5 Step 7. |
| 3 → 6 | T3 creates `Meeting.swift` with `Occasion`; T6 appends `MeetingCoordinator` | Clean. |
| 3, 6 → `Phrasebook.swift` | T3 adds `.birthday` pool; T6 adds `meetingPairs` | Clean — disjoint additions. |
| 3, 6 → `PetInteraction` bubble method | both name `show(_:)` | **CONFLICT (R2).** No `show(_:)` exists. The real method is `private func showBubble(_:)` at PetInteraction.swift:196. T6 calls it from `AppDelegate`, so it cannot stay private. |
| 4 → 5, 6 | T4 creates `Pet`; T5 and T6 consume `pet.profile`, `pet.walker`, `pet.interaction` | Clean — signatures match the T4 Interfaces block after the `isStationary(spriteSet:)` fix already applied to the plan. |
| 6 → `Behavior.swift` | T6 adds `Walker.reverse()`, used only by T6 | Clean. |
| 1, 5 → `ConfigTests.swift`; 3, 6 → `MeetingTests.swift` | append-only within `run()` | Clean. |

### Per-task internal-consistency rows

| Task | Own tests vs own code / files created vs files touched | Finding |
| --- | --- | --- |
| 0 | shell only, no code | Clean. |
| 1 | probe decides the branch; both branches written in full; test asserts the config key only | Clean. |
| 2 | no tests (AppKit window, verified by hand per §9) | Clean — §9 permits this where success is visual and the steps say exactly what to look for. |
| 3 | tests assert `Occasion` + birthday pool; code adds `.birthday` to `PetPrompt` | **CONFLICT (R4).** `PetPrompt.allCases` has 5 use sites, not the 1 the plan patches. **CONFLICT (R5).** The dedication fires on the first `say()` of the day — including inside `--interaction-test`, which would consume it and write `state.json` as a test side effect. |
| 4 | no new tests; gated on existing suite + 3 self-tests staying green | Clean — this is the point of the task. |
| 5 | tests assert the `pets` array; code builds pets from it | Clean. |
| 6 | tests assert overlap/cooldown/pairs; `Harness.bail` called as `return Harness.bail(...)` | **CONFLICT (R3).** Existing suite calls `Harness.bail(...)` bare (GeometryTests.swift:118 and 8 others). |
| 7 | docs only | Clean. |

## Rulings

Ruling: R1 — `PetProfile` gets a hand-written `init(from:)` defaulting every missing key (`name`/`userName` to nil, `color` to `CatPalette.default.id`), mirroring `PetConfig.init(from:)`. — A partial `pets` entry must not throw away the whole config file; Global Constraints say bad values are clamped and logged, never fatal. — If wrong: a typo'd coat key silently becomes orange instead of failing loudly, which is the spec's stated policy anyway. Low cost.

Ruling: R2 — the bubble method is `showBubble(_:)`, and Task 6 requires dropping its `private`. Task 3 calls it unqualified from inside `PetInteraction`; Task 6 calls `pet.interaction.showBubble(...)`. — Two code paths that present a bubble would drift, exactly as the spec says about the Accessibility prompt. — If wrong: one method widens from private to internal inside the app target. Negligible.

Ruling: R3 — tests call `Harness.bail(...)` bare, not `return Harness.bail(...)`. — Matches all 9 existing call sites; `Never` in return position is legal but unlike the surrounding code. — If wrong: nothing; both compile.

Ruling: R4 — only the menu-building loop (`PetInteraction.swift:138`) filters `.birthday`. The other four `PetPrompt.allCases` sites correctly include it: the launch log's prompt count, `--interaction-test`'s per-prompt bubble check, and two `PhrasebookTests` loops that assert every pool is well-formed. — The birthday pool should be covered by those checks; suppressing it everywhere would leave the one pool that matters on the day untested. — If wrong: the launch log says 5 prompts instead of 4, and one extra case runs in the interaction test. Benign.

Ruling: R5 — the dedication and the birthday swap must be suppressed under any self-test. `PetInteraction` gains `var isSelfTest = false`, set from `LaunchOptions.isSelfTest` when the pet is built; `say(_:)` skips both the dedication branch and the `.hello`→`.birthday` swap when it is true. — Otherwise running `--interaction-test` on the morning of the birthday consumes the once-a-day dedication and writes `state.json`, so Philippine's first real click gets an ordinary hello. This is the one pre-flight finding that damages the gift itself. — If wrong: the interaction test never exercises the dedication path, which is covered by `MeetingTests` and by hand at Task 3 Step 10 instead. Low cost.

Ruling: R6 — work proceeds on a branch `m11-two-cats` in the primary working copy, not a git worktree. — Every verification step in this plan runs the real app against state a worktree does not isolate: `~/Library/Application Support/DockPet/`, and an Accessibility grant keyed to the code signature (SPEC §8.6). The repo is created by Task 0 and has no other work to conflict with; the baseline commit on the default branch stays as the escape hatch. — If wrong: the branch is trivially rebased or merged. Negligible.

## Progress

Ruling: Task 0 reviewed by direct verification rather than a dispatched task reviewer. — The task produces no application code: one `.gitignore` and a baseline commit of pre-existing files. A review seat would have nothing to review, and I ran the same checks a reviewer would. — If wrong: negligible; the baseline is verified by build, test suite, `git ls-files` and `git status`.

Task 0: minor (deferred): the implementer wrote a 70-line `.gitignore` where the brief specified 4 lines. Verified correct — every ignored path is regenerated by a committed generator (`makesprite.swift` writes all four cat sheets, `makeicon.swift` writes the icon, `bundle.sh` runs both when missing), and nothing hand-authored is ignored. Accepted rather than looped on.

Ruling: R7 — Task 1's probe as written cannot answer its own question and must be replaced. — `SMAppService.mainApp` resolves against the *running process's main bundle*; a loose `swiftc` binary has none, so the plan's `/tmp/loginprobe` returns `.notFound` unconditionally and would wrongly select the launchd fallback. The probe instead implements the `SMAppService` branch, runs `./bundle.sh`, and reads the `[config] launch at login:` line that Step 7 already prints from inside the real bundle. — If wrong: the implementer reports the observed status and I re-rule; no code is thrown away either way, since Step 7's log line is in the plan regardless.

Task 0: complete (commits f002e1b..8f4bdcc, verified directly — build clean, all 358 checks passed, branch m11-two-cats)
Task 1: implemented on SMAppService (commit 172c5f9). R7's replacement probe answered: `[config] launch at login: enabled` from inside the signed bundle, disable path `not registered`, cross-checked with `sfltool dumpbtm`. The launchd fallback is NOT in the code and should not be.

Task 1: HAZARD — a second Claude session is editing this same checkout (README.md, RenderTest.swift, CatPalette.swift, CatPaletteTests.swift, makesprite.swift carry uncommitted changes; matches the standing memory note that parallel sessions break swift build). Task 1's commit contains only its own 5 files — verified with `git diff --stat`. Transient test FAILs during Task 1 traced to that session's in-progress edits, not to this work. Watch for this on every later task: check `git diff --stat BASE..HEAD` names only the task's files before packaging a review.

Task 1: implementer-flagged concern carried to review — `applyConfig` is not self-test-guarded (only the launch-time call is, per the brief's literal Step 7), so a self-test could transiently toggle-then-restore the login item.
Collision analysis vs the parallel session (docpets-78), checked while Task 1 review ran:
  - Their files: README.md, RenderTest.swift, CatPalette.swift, CatPaletteTests.swift, makesprite.swift. The CatPalette/RenderTest diffs are additive (+19/+24) — they appear to be adding a coat colour.
  - `RenderTest` is a standalone `enum` (RenderTest.swift:16), NOT an AppDelegate extension, and references none of the seven properties Task 4 moves onto `Pet`. Task 4's refactor therefore does not need to touch it. Low risk.
  - Genuine overlap is README.md only, which Task 7 rewrites. Decide at Task 7; do not guess.
  - Every dispatch must name the exact files to `git add`. Never `git add -A` in this checkout.
Ruling: Task 1 finding 1 (plan-mandated) — the reviewer is right and the plan is wrong. The brief's Step 7 specifies the `applyConfig` addition with no self-test guard, so `--interaction-test` calls `applyConfig` with a synthetic `PetConfig` that defaults `launchAtLogin` to true, really registering the user's login item and unregistering it again — and leaving it registered if the test aborts between the two. That directly violates the invariant the task's own new comment states ("a test run must not rewrite the user's login item as a side effect of checking something else"). Fix it: guard the `applyConfig` call site with `options.isSelfTest`, the same way the launch-time call already is. — If wrong: a self-test would no longer exercise the login-item path, which is covered by Step 9's hand verification instead. Low cost. The plan is authoritative only where the spec does not contradict it, and here it contradicts itself.

Ruling: Task 1 finding 2 — fix the comment, not the code. `LoginItem.swift`'s doc comment describes a launchd fallback that R7 correctly caused not to be written. R7 stands; the comment is stale template text and must say plainly that SMAppService was verified against the signed bundle and no fallback exists. — If wrong: nothing; it is a comment. But a maintainer debugging a registration failure would hunt for a branch that never existed.

Task 1: minor (deferred): `SettingsWindow.swift:248-260` — `simulate(...)` gained a `launchAtLogin: Bool?` parameter no `--settings-test` assertion exercises. Consistent with the file's existing per-field pattern; dead surface area, not over-engineering. Triage at final review.
Task 1: fix round 1/5 (2 addressed, 0 open — applyConfig self-test guard; LoginItem stale fallback comment; commits 172c5f9..718e05d)
Task 1: complete (commits 8f4bdcc..718e05d, review clean)

Ruling: R8 — all work moves to `main`; branch `m11-two-cats` deleted. — Direct user instruction ("carry on, but always work on the main or master branch"), which overrides the subagent-driven-development skill's isolated-branch requirement and supplies the explicit consent that skill asks for. Saved to memory as `work-on-main-branch`. Moved with `git checkout -B main`, NOT a checkout, so the parallel session's 8 uncommitted files were never touched. — If wrong: history is linear and trivially re-branched.

Ruling: R9 — carry on despite the collision with docpets-78, per direct user instruction. Mitigations, binding on every remaining dispatch: (1) commit named files only, never `git add -A`; (2) never run `git checkout --`, `git stash`, `git reset` or any tree-restoring command on ANY file — the other session's work is uncommitted and unrecoverable; (3) when a shared file (AppDelegate.swift, Config.swift, ConfigTests.swift) holds both my hunks and theirs, commit only my own hunks via a hand-built patch, as Task 1's implementer did. — If wrong: their olive-coat work is destroyed with no way back. This is the one risk in the run with no undo, so the mitigation is mandatory, not advisory.

Task 5 WATCH: docpets-78 is making `olive` the default coat and asserting a fresh install is already recoloured. Task 5 Step 8 picks the second cat's coat as "the first `CatPalette` entry that is not pet 0's". That still works under a changed default, but the test expectations in Task 5 Step 1 use "orange" as `PetConfig.default.color`. Re-read the real default before writing those assertions.
Task 2: implemented (commit 42e6cef, on main). Hunk isolation verified by me: `git diff --stat 718e05d..42e6cef` names only AppDelegate.swift and the new OnboardingWindow.swift; all 8 of the parallel session's files remain modified-and-uncommitted, and their olive hunk is still present in the working tree.

Task 2: accepted concern — the implementer could NOT perform Step 6's GUI verification (revoking the Accessibility grant and watching the window). It gave indirect evidence (a freshly-built never-trusted binary that TCC treats as ungranted, logging `accessibility: not granted`) and reported honestly rather than claiming an eyeball confirmation. Correct behaviour, but the consequence stands: NO HUMAN HAS SEEN THIS WINDOW. Carried into the review dispatch as the reason to scrutinise lifecycle and trigger conditions.

Ruling: R10 — accepted the implementer's deviation from the brief's literal `AXIsProcessTrusted()` in favour of `DockTiles.isTrusted`. — I instructed it to match the codebase's existing idiom over the brief's stale snippet, and every other call site in the repo uses the wrapper; two spellings of the same check are how drift starts. — If wrong: the wrapper could mean something subtly different from the raw call, so the reviewer was asked to verify its definition as a named risk. Low cost.

USER-VERIFICATION REQUIRED before hand-over: the onboarding window cannot be verified by any agent in this environment (no GUI, `screencapture` fails). Tiago must run Step 6 himself — revoke DockPet in System Settings > Privacy & Security > Accessibility, launch the app, and confirm the window appears, the button opens the pane, the status line turns green, and the window closes itself. This is the single most important manual check in the project, because it is the first thing Philippine will see.
Task 2: reviewer ⚠️ RESOLVED by me — `LaunchOptions.isSelfTest` is `renderTest || settingsTest || menuTest || dockBounds || interactionTest`, i.e. the OR of all five test flags, not four. The onboarding gate cannot fire during any self-test. Not a gap.

Ruling: Task 2 finding 1 (plan-mandated) — the reviewer is right and my plan's window code is wrong. A `wrappingLabelWithString:` label inside a `.leading`-aligned NSStackView has no trailing constraint and no `preferredMaxLayoutWidth`, so it can lay out at its natural near-single-line width and be clipped by a non-resizable 420pt window; and the hardcoded 190pt height leaves too little budget once the body wraps to 3-4 lines. Fix: constrain the body's width AND let the window size itself to the stack's fitting height instead of hardcoding 190. — Guessing a second fixed height would repeat the same mistake with a different number. — If wrong: the window is a little taller or shorter than intended, which is visible and trivially adjusted. Cost of NOT fixing: the one message the task exists to deliver renders clipped on the first screen Philippine ever sees, and no test in this repo would catch it.

Ruling: Task 2 finding 2 — fix it, though I would have rated it Minor. `onboarding` is never set back to nil, so a hidden NSWindow is retained for the app's life and every 500 ms poll dereferences it. Guarded and cheap, so not a correctness bug — but the fix is two lines and I am not going to spend a review round arguing severity down. — If wrong: nothing. — Recording the disagreement rather than silently downgrading it.

Ruling: Task 2 minor (keyEquivalent) — FOLDED INTO THE FIX rather than deferred. `orderFrontRegardless()` without making the window key means the default button's Return key may not fire until the user clicks into the window. SPEC §3's never-make-key rule constrains the *pet* window only; this is an ordinary titled window and may be key. One-line change on the highest-stakes surface in the project. — If wrong: the window takes focus on launch, which is what a permission prompt should do anyway.

## RESTRUCTURE (user asked for more parallelism, 2026-08-25)

Ruling: R11 — the plan's task order is re-cut along an AppKit boundary rather than followed literally, to parallelise. Tasks 3 and 6 are each half pure `DockPetCore` logic (Occasion, the birthday pool, MeetingLine/meetingPairs, MeetingCoordinator, Walker.reverse, and all their tests) and half AppKit wiring (StateStore, PetInteraction, AppDelegate). The pure halves share no file with anything now in flight, so they run as one combined agent (P) immediately, concurrent with Task 2's fix round. — The plan's sequence was chosen for reviewability, not because these have a dependency; splitting on the DockPetCore/AppKit line is the same split the spec already enforces by linkage (§5: DockPetCore links CoreGraphics only). — If wrong: the pure work lands ahead of its consumers and sits unused until the wiring tasks arrive, which costs nothing. The task briefs and review gates are unchanged; only the batching is.

File ownership while parallel (no two live agents share a file):
  Task 2 fix  : AppDelegate.swift, OnboardingWindow.swift
  Agent P     : Meeting.swift (new), Phrasebook.swift, Behavior.swift, Config.swift,
                MeetingTests.swift (new), BehaviorTests.swift, Tests/main.swift
  Blocked until both land: Task 3-AppKit (StateStore, PetInteraction, AppDelegate props)
Task 2: fix round 1/5 (3 addressed, 0 open — body width constraint + fittingSize-based window sizing; onboarding bounded lifetime; makeKeyAndOrderFront; commits 42e6cef..0f61fe1)
Task 2: complete (commits 718e05d..0f61fe1, review clean). Runtime evidence: stack.fittingSize=(400,178), body.frame width 384 height 45 = 3 wrapped lines. The re-reviewer separately confirmed the `isFinished && !isVisible` gate cannot deallocate the window during the 2.5s post-grant message.

Ruling: R12 — the `onboarding = nil` clear is gated on `isFinished && !isVisible`, not `isFinished` alone. — The implementer chose this and the re-reviewer verified the timeline: `isFinished` flips at T=0 but the window stays visible until the delayed `orderOut` fires at T≈2.5s; clearing on `isFinished` alone would drop the last strong reference mid-message. — If wrong: the window reference lingers one extra poll tick. Negligible either way, and the stricter gate is correct.

## Next stage: AppKit-B (Task 3's wiring + Task 4's refactor, merged)
Blocked on Agent P landing `Occasion`, `Phrasebook.birthdayLines` and `PetProfile`.
Files: StateStore.swift (new), PetInteraction.swift, Pet.swift (new), AppDelegate.swift.
No overlap with Agent P's files.
Agent P: complete pending review (commits 0f61fe1..d34bc98 — f47907f occasions/birthday/pets, d34bc98 the meeting). 480 checks pass; each commit verified green in isolation (409, 466). Hunk isolation verified by me: `git diff --stat` names only its 8 files, and the parallel session's 8 files are all still modified-and-uncommitted.

Ruling: P deviation 1 ACCEPTED — the plan's leap-day test used `date(2026, 2, 29)`, and 2026 is not a leap year, so the test asserted against a date that does not exist. Changed to 2024. — My plan was simply wrong; a Gregorian calendar would either roll the date or return nil, and the test would have been meaningless either way. — If wrong: nothing; 2024 is a leap year and the assertion now tests what it claims to.

Ruling: P deviation 2 ACCEPTED — the plan's cooldown test called `advance(by: 59)` in a single call while the plan ALSO required `advance` to clamp to `BehaviorMachine.maximumStep` (1.0s). Those two requirements contradict: with clamping, one call advances one second and the test fails. Changed to a loop of small steps, mirroring how `BehaviorTests` already handles the same tension for `BehaviorMachine`. — Two of my own requirements were mutually exclusive and the agent picked the one the spec cares about (bounded steps, so a stalled process cannot burn the cooldown in one tick) over the one that was merely convenient to write. — If wrong: the cooldown is tested through many small steps rather than one large one, which is closer to how it actually runs. The reviewer was asked to verify the clamping really exists, since this ruling depends on it.

Agent B: dispatched on opus (commits pending) — Part 1 the gift wiring (StateStore, PetInteraction), Part 2 the Pet extraction. AppDelegate.swift baseline before refactor: 1341 lines.
Agent P review: Approved. Every binding signature verified byte-for-byte against the brief; the reviewer independently confirmed `BehaviorMachine.maximumStep = 1.0`, which is what makes deviation 2 genuine, and confirmed `meet()` renders the opener with the replier's name and vice versa. Both my deviation rulings stand.

Ruling: P review finding 1 (plan-mandated) — FIX IT. My own test at MeetingTests.swift:746-748 is vacuous: its third disjunct `!opener.contains(nameSlot)` was already asserted true two lines earlier and is true after ANY successful render, so the whole `||` chain passes regardless of which name went into which line. It would pass identically if `meet()` swapped openerName and replierName. The reviewer verified by inspection that the implementation is correct — but the behaviour is load-bearing and subtle (each line must address the OTHER cat; a swap reads perfectly fine in isolation) and it is the actual charm of the feature. A test that cannot fail on that is worth nothing. — If wrong: a slightly stricter test. Cost of not fixing: a future edit silently swaps the two names and nothing catches it.

Ruling: P review minor 2 — FOLDED INTO THE FIX. `Occasion.parse`'s range check is the one item on the review checklist the suite never actually confirms; `"13-01"` and `"02-30"` are two lines of test. Note `"02-30"` IS accepted by design (parse range-checks 1...31 generically, not per-month) — pin that documented behaviour rather than change it.

Ruling: P review minor 1 — DEFERRED to final review. The reroll-without-repeat logic is duplicated between `Phrasebook.reply` and `MeetingCoordinator.meet`. Six lines, and the duplication matches the codebase's existing single-file locality. Extracting a shared helper now would touch a file Agent B is not in but a later agent might be. — If wrong: the two copies drift. Low risk; both are short and adjacent in purpose.

Task 1: minor (deferred): SettingsWindow `simulate(launchAtLogin:)` unexercised.
Agent P: minor (deferred): reroll-without-repeat duplicated across Phrasebook.reply and MeetingCoordinator.meet.
Agent P: fix round 1/5 (2 addressed, 0 open — vacuous meeting-exchange assertion replaced with name-direction checks; Occasion.parse boundaries pinned; commit c990e33)
Agent P: complete (commits 0f61fe1..c990e33, review clean). The swapped-name experiment was run and verified: swapping openerName/replierName in meet() made both new checks FAIL, and the revert was byte-identical and never staged — the re-reviewer independently confirmed Meeting.swift is absent from the fix diff, so the temporary swap did not leak into history.

Agent C brief written and queued at agent-C-brief.md. Blocked on Agent B landing the Pet extraction.
  Files: AppDelegate.swift, SettingsWindow.swift, PetInteraction.swift (delegate signature only).
  Carries two overrides of task-6-brief Step 6 (showBubble not show; advance() clamp not to be compensated for)
  and the per-interaction dispatch warning: without it, clicking cat 2 sleeps cat 1 and greets with cat 1's name.
Agent B: DONE_WITH_CONCERNS. Commits 9b12d74 (Part 1 gift wiring) and 9612038 (Part 2 Pet extraction). AppDelegate 1341 -> 1080 lines (-19.5%). 485/485 checks; all three self-tests exit 0. Hunk isolation verified by me: the parallel session's 8 files are still modified-and-uncommitted.

Ruling: B concern 3 ACCEPTED — B created a fifth file, `PetSelfTests.swift` (352 lines), outside its brief's file table. — A straight extraction left AppDelegate at 1413 lines, LARGER than the 1341 it started at, which defeats a stated goal of the step; moving the three pet-facing self-tests out brought it to 1080. It deliberately left `runSettingsTest` in place because that is exactly where the parallel session is editing — a genuinely thoughtful call. — If wrong: one more file in Sources/DockPet/, easily folded back. Flagged to the reviewer as unbriefed scope rather than hidden.

Ruling: B concern 1 DEFERRED TO AGENT C — the dedication branch returns early, so a walking cat keeps walking under its own dedication, and `lastReply` is never set so the one sentence that matters is invisible in the verbose log and to `--interaction-test`. B was right not to improvise: the brief said verbatim, and I would rather it flag than freelance. C owns `PetInteraction.swift` and fixes it there. — If wrong: two lines move between agents. — This is a real defect in MY plan's Step 8, not in B's work.

Ruling: B concern 2 EXPECTED, NOT A DEFECT — `interactionForcePetState` still forces `primaryPet`. Correct with one cat, wrong with two, and Agent C's brief already carries the fix as its most-likely-bug warning. B additionally made `--interaction-test` assert on `pet.behavior.state`, so this will fail loudly when the second cat lands rather than passing silently. That is better than fixing it early.

INSTALL SIDE EFFECT (surface to user): `install.sh` is pre-existing — it is in the baseline commit 8f4bdcc, added to the tree by the parallel session before Task 0 ran. Agent B executed it, so `/Applications/DockPet.app` now exists and PID 23192 is running from it, built from MID-REFACTOR code, and it triggered a system Accessibility prompt. Not destructive and arguably where the app should live, but it is an outward side effect nobody asked for. Must be rebuilt and relaunched from final code before hand-over, or killed.
Agent B review: APPROVED. The reviewer compared poll(), animationTick(), suspensionReason(), applyBehaviorState() and position() against their old forms line by line and found no ordering, early-return or first-tick change; confirmed exactly one animation timer and one 500ms poll with no timer inside Pet; traced the whole object graph and found no retain cycle (PetInteraction.delegate/.window/.view and PetView.clickDelegate are all weak); and walked every self-test assertion string across the move to PetSelfTests.swift confirming none was lost and two got stronger. Ruling R13 (B's unbriefed fifth file) upheld on the merits: the seam is defensible and leaving runSettingsTest alone was the right risk call.

Ruling: B review found THREE consequences of my plan's dedication early-return that I had missed, all going to Agent C. — (a) On the birthday itself the `.hello -> .birthday` swap runs FIRST and is then discarded by the dedication's early return, so on the one morning both features fire the user must click twice to ever see a birthday line; the ordering is backwards. (b) The dedication swallows `.nap` outright — the first click of the day on "Take a nap" shows the dedication and the cat does NOT sleep, because `forcedState` is never applied below the return. The user's chosen action is dropped, not deferred. (c) `lastGreetedDay` is stamped BEFORE `showBubble`, which returns silently if the delegate is nil — unreachable today, but it makes "the day is consumed with nothing shown" a one-line change away. — These are defects in MY plan's Step 8, not in any agent's work. (b) is the worst: silently dropping a user's action is not a cosmetic bug, and it happens on first click of every day. — If wrong: the dedication fires on a later click than intended, which is visible and adjustable.

Ruling: B review Important 1 -> AGENT C, and partly already in its brief. Per-pet coat is stored and reported at three sites (launch log, verbose log, --render-test) but the sprite set is loaded once globally, so all three will confidently report a coat the cat is not wearing the moment pet 1 differs. C's brief already requires one recoloured SpriteSet per distinct coat, which fixes the cause; ADDED to C: make --render-test ASSERT the relationship rather than print it, since it currently prints the value and excludes it from the ok boolean.

Ruling: B review Important 2 -> AGENT C. `Pet` owns an NSWindow and a global NSEvent monitor with no teardown path. Harmless while the array is built once, but C makes `pets` come from config and rebuilds on Settings change; an orphaned ordered-in window leaves a cat on the Dock forever with a live mouse monitor behind it — a leak that looks exactly like a rendering bug. Five lines now while the invariant is still trivially true.

Ruling: B review Important 3 -> DEFERRED to a follow-up agent, not dropped. The dedication, the birthday swap and all of StateStore have ZERO automated coverage in both directions: `isSelfTest` is true for all five modes, and GeometryTests links DockPetCore only, so the suite cannot import PetInteraction or StateStore at all. Nobody has verified the POSITIVE — that the dedication is ever said — on the feature the milestone exists to deliver. C is already carrying a lot and this needs a new launch mode plus a temp-dir StateStore; doing it after C keeps the two out of the same files. — If wrong and it never gets done: the centrepiece ships verified only by hand. Tracked as MUST-DO, not deferred-minor.

Deferred minors from B review (for final review triage): pet.index used as array subscript with the invariant unwritten (PetSelfTests.swift:74,111,113); 13 members widened private->internal where private(set) would do; Pet's two-phase init; statusSummary describes primaryPet only; one global mouse monitor per pet rather than one app-level monitor fanning out; Pet.profile is var but never mutated; @discardableResult on advanceBehavior whose return is load-bearing.
Agent C: DONE_WITH_CONCERNS. Commits f19402f (Part 0 dedication), 070873c (Part 1 second cat), 2137146 (Part 2 meeting). 485 checks; all three self-tests exit 0 both single-cat (43/20/38) and two-cat (43/25/73).

Ruling: C concern 3 is WRONG — verified by reading the code myself rather than accepting it. `MeetingCoordinator.advance` clamps with `BehaviorMachine.maximumStep`, which is 1.0 (Behavior.swift:154), NOT `Walker.maximumStep` which is 0.25 (Behavior.swift:42). C conflated the two. With the poll's dt of ~0.5s, `min(0.5, 1.0) = 0.5`, so the cooldown accumulates in real time and 60s means 60s. No doubling, no bug, no action. — Agent P's reviewer had already verified this same line and value independently; two agents agreeing against one is not why I checked, the code is.

Ruling: C's dedication-vs-birthday ordering ACCEPTED — it decides the dedication BEFORE the `.hello -> .birthday` swap, so on the birthday the swap never happens and the greeting is still unspent on the next click. That is better than what I asked for: I said "the greeting must not be lost", and deciding first means nothing is ever discarded rather than discarded-and-restored. Probe-verified by C that the nap still fires on the dedication click. — If wrong: on the birthday the first click is the dedication and the second is the birthday line, which is the intended feel anyway.

Task 2/C: --settings-test currently exits 1 on two checks. Confirmed NOT ours: they are the parallel session's uncommitted `olive` assertions running against the real config.json, which pins `orange`. C correctly left them alone, and separately fixed one stale assertion in that function that IS ours (`config == .default` can no longer hold now that `validated()` fills in `pets`).

RESIDUAL, for the user: still nobody has seen two cats, a meeting, or the onboarding window. Every agent has reported honestly that Accessibility is not granted in its environment, so `locate()` returns `.accessibilityNotGranted` and no pet is ever placed. C verified per-pet names, per-pet naps and the full meeting sequence via temporary probes (removed before commit), but the visual and positional behaviour is unverified by anyone.
Agent C review: NEEDS FIXES. Part 0, per-interaction dispatch, teardown and per-pet sprite sets all verified correct and praised. Three defects found that no test in this environment could catch:

Ruling: C review Critical 1 — FIX. `SettingsWindow.currentValues()` takes `secondColorPopup.selectedItem ?? Self.coatUnlike(coat)`, but `fillCoats` always ends with `selectItem(at: index ?? 0)`, so `selectedItem` is never nil and the fallback is unreachable dead code. The new cat therefore always gets `CatPalette.all[0]`, which at HEAD IS the default coat — tick "A second cat" on a default config and you get two identical orange cats, exactly the outcome the brief singled out. Worse: this diff's OWN new assertion (`pets[1].color != soloCoat`) fails deterministically on the committed tree, and passed in the implementer's run ONLY because its build included the parallel session's uncommitted olive-first catalogue where `all[0] != "orange"`. — The reviewer demonstrated this from initial values without needing a screen. — If wrong: nothing; `coatUnlike` already exists and is correct, it is simply never called.

Ruling: C review Critical 2 — FIX, HIGHEST PRIORITY IN THE PROJECT. `currentValues()` omits `birthday` and `dedication`, which default to nil; `applyConfig` assigns `config = newConfig` wholesale and `ConfigStore.write` replaces the file. So opening Settings and touching ANY control silently erases the birthday and the dedication from disk, with no way back short of hand-editing JSON. Philippine nudges the speed slider and the entire gift layer this milestone exists for is gone. — The shape predates the diff, but the function was rewritten here in a file this task owns, and the author applied exactly the right reasoning one field over (the `loadedPets` comment explains at length why a cat's name must be carried through). — If wrong: nothing. This is the single most damaging bug found in the run.

Ruling: C review Important 3 — FIX. Both pets construct `Walker(speed:)` with distance 0 and no offset, so on a real Dock they launch superimposed, and on the FIRST located poll `haveMet` (`a.intersects(b) || a == b`) is true for identical frames while `sinceLastMeeting` starts at `cooldown` — so two stacked cats immediately spend the meeting on themselves, burning the 60s cooldown on something nobody can read as a meeting, and only separate once their behaviour machines diverge. `rebuildPets` reproduces it on demand and additionally teleports the existing cat back to the left end. — If wrong: the cats start spaced out, which is what anyone would expect anyway.

Ruling: C review Important 4 — FIX. `--interaction-test` hand-builds a `PetConfig` with `pets: []`, which `cast(of:)` normalises to one profile; under a two-cat config that triggers `rebuildPets` mid-test, so the captured `primary` is a closed detached pet and the userName check then passes via the nil-pet FALLBACK rather than against a live pet. And `grep userName PetSelfTests.swift` returns exactly one hit: there is NO assertion anywhere that pet 1 greets with pet 1's name. The nap half of the headline two-cat bug is covered per pet; the name half rests entirely on code reading.

Ruling: C review minors 5, 7, 9, 6 — FOLDED INTO THE FIX (one to three lines each, and each is a stated-behaviour mismatch rather than polish): opener is hardcoded `pets[0]` rather than the left-hand cat despite `aIsLeft` being computed right there; the delayed reply re-checks membership but not `isTalking`, so a click during the 1.5s gap gets its bubble stolen by the meeting — the inverse of the precedence rule; coat reload compares SETS of palette ids, so a simultaneous two-coat swap is a no-op and each cat keeps the other's art while every log claims otherwise (latent, and exactly the failure Part 0b exists to prevent, reintroduced one level up); and `rebuildPets` empties `pets` before it knows the sheets will load, so a load failure takes every cat away while the doc comment three lines up claims it cannot.

Deferred to final-review triage (C review minors 8, 10, 11, 12): reversing both pets only separates a head-on overlap; --settings-test's new coat checks are coupled to the user's real config; the meeting is invisible to --verbose's `saying=` field; the reverse is applied before the reply rather than after, so "both stand, turn around" is not literally the specified order.
Agent C: fix round 1/5 (6 addressed, 0 open — commit 29e68a4). It proved three fixes have teeth by deliberately breaking them (disabling the coat chooser -> "both are olive"; reverting the resolver to primaryPet -> 3 name checks fail with Cat0Name), and it unprompted CORRECTED its own earlier misattribution: one of the three --settings-test failures it had blamed on the parallel session was its own, masked by their olive-first catalogue being in its build. Now 2 of 44 fail and both are genuinely theirs.

Ruling: R14 — Task 7 edits README.md ADDITIVELY around the parallel session's uncommitted text rather than rewriting the block, and if a hunk unavoidably carries their text, it is committed WITH their text. — Their edits (the `"color": "olive"` line in the config example and the rewritten `color` bullet) sit in exactly the block my plan's Task 7 rewrites. This is a genuine content conflict, and the user has already ruled once to carry on regardless, so re-asking would re-litigate a settled decision. Committing their text preserves their work in git, which is strictly safer than leaving it uncommitted; they lose nothing either way. Rewriting or reverting their prose would not be safe, and is forbidden in the dispatch. — If wrong: a README commit contains two paragraphs I did not write, attributed to a commit of mine. Trivially amendable, and their work survives.

Remaining after these two land: (1) the deferred MUST-DO on dedication/StateStore positive-path coverage — reassess after the re-review, since C's Critical-2 fix added a --settings-test dedication-survives-a-slider assertion which may already cover part of it; (2) the final whole-branch review; (3) rebuild /Applications/DockPet.app from final code and relaunch, since Agent B left it running from mid-refactor code.
Agent C: re-review verdict — ALL FINDINGS ADDRESSED. Critical 2 took the structurally-safe shape (currentValues() starts from the delegate's live config and mutates only the eight fields the window owns, so a future field is preserved by default rather than needing enumeration) and the new --settings-test assertion reads config.json back OFF DISK rather than checking in-memory state. Important 3's stagger is a real guarantee, not a probability: pet 1 starts at a beyond-any-strip distance that Walker.clamp pins to the true far end on first poll. Teeth verified on both claimed experiments: the assertions exist and genuinely discriminate, and no existing assertion was weakened to pass.

Ruling: two residuals from the re-review go to the FINAL REVIEW fix wave rather than another round here. (a) `resetToDefaults()` still takes the fragile enumerated shape — it rebuilds PetConfig.default and explicitly re-copies birthday/dedication, so a third gift-layer field would be silently wiped by Reset. Same class as Critical 2 but far narrower: Reset is a rare explicit action, not every settings change. (b) `startingPlace` has an untested short-strip edge case where near and far ends could coincide, and a newly-added cat's spawn is not checked against a surviving carried cat's position. Outside any real Dock geometry. — Neither reproduces the class of bug its finding targeted, and a sixth round on Agent C costs more than folding two small items into the wave that is coming anyway. — If wrong: (a) bites only if someone adds a fourth gift-layer key AND the user hits Reset; (b) bites only on a Dock narrower than two cats.
Task 7: complete (commit 12d499f). README documents launchAtLogin, pets, birthday, dedication; adds "Two cats" and "Installing it on someone else's Mac"; corrects the stale "unsigned and unnotarised" claim, which SPEC §8.6 [M11] had already amended. The parallel session's two olive edits are intact verbatim inside my commit, per R14 — their work is now preserved in git rather than sitting uncommitted.

CONFIRMED GAP (the deferred MUST-DO still stands): --settings-test now covers the dedication's PERSISTENCE thoroughly (AppDelegate.swift:654,659,666,673,790 — including reading config.json back off disk), but NOTHING asserts the dedication is ever SAID. PetInteraction.swift:219 (the StateStore read that gates it) and :258 (the write that stamps the day) have no coverage in either direction. Nobody has verified the positive path on the feature the milestone exists to deliver. Dispatching Agent D for it now.

## FINAL WHOLE-BRANCH REVIEW: "Ship with these fixes first"

Ruling: FINAL C1 — MUST FIX, and it is the single most important finding of the entire run. On a Mac with NO `config.json` — which is exactly the hand-over machine — `ConfigStore.load()` returns `PetConfig.default` WITHOUT calling `.validated()` (ConfigStore.swift:41 and :55). `PetConfig.default`'s `pets:` defaults to `[]`, so `AppDelegate.swift:204`'s `let profiles = config.pets` is empty, no sprite sets load, and the guard at :218 calls `exit(1)`. The app dies silently on first launch: no window, no menu bar item, nothing. The SECOND launch works, because launch #1 wrote the defaults to disk before dying. A non-developer will never discover that by trying again. It also breaks SPEC §1's "bad values are clamped and logged, never fatal" — a hand-edited config.json with a syntax error is now fatal. Nobody caught it because all five self-test modes run on a machine that already has a config.json. — The comment at :204 asserts "validated() guarantees this is never empty"; the premise is false on exactly those two paths. Fix uses `Self.cast(of:)`, which already exists at :1454 for this case. — If wrong: nothing; cast(of:) is the documented normaliser.

Ruling: FINAL I1 — MUST FIX. The onboarding window's only button calls `AXIsProcessTrustedWithOptions([prompt: true])`, which presents its alert ONLY while no TCC record exists. Once the user has answered or dismissed it — including by clicking Deny — the button labelled "Open Accessibility Settings…" is permanently inert, the status line still reads "Waiting for permission…", and the cat never appears. That is precisely the state SPEC 11a says this window exists to prevent. The label also overpromises: nothing in Sources/ opens System Settings. Compounding it, the launch-time requestTrust and the window fire on the same run-loop turn, so a first-run user gets the system TCC alert AND DockPet's window activating over it. Fix: open the pane by URL, and suppress the launch-time prompt when the window is about to show. MenuBarItem's "Grant Accessibility…" has the same dead-button problem.

Ruling: FINAL I2 — MUST FIX. `applyConfig` drops `LoginItem.setEnabled`'s Result (AppDelegate.swift:1249-1251). SPEC 11a requires a registration failure be "logged and clamped to off, never fatal"; neither happens, so a user who ticks the box on a machine that refuses registration gets a ticked box, `launchAtLogin: true` on disk, and no indication it did not take — a plausible outcome on a Mac that does not trust our local signing identity, and exactly what the clamp rule was written for.

Ruling: FINAL I3 — MUST FIX. The meeting shows the opener's bubble and, 1.5s later, the replier's, and nothing dismisses the first. Both bubbles sit at the same y and their pets are 34-64pt apart, while a typical line is ~215pt wide — so for ~1.5s two opaque bubbles with two tails overlap. The milestone's headline feature, never seen by anyone, would read as a rendering glitch. Fix: dismiss the opener's bubble before showing the reply.

Ruling: known item 10 PROMOTED to must-fix, per the reviewer. The meeting is invisible to `--verbose` (`showBubble` never sets `lastReply`, and no `logLocation` fires for the forced sit), so a snapshot mid-exchange prints `talking=true` with no `saying=` and no state line. Since nobody has ever seen the meeting, --verbose is the ENTIRE observation channel. Two lines.

Ruling: FINAL M6 folded into the fix wave — the onboarding window is `.closable` and its reference is cleared only on `isFinished && !isVisible`, so a user who closes it by hand without granting leaves it retained forever while the poll re-evaluates twice a second. Reachable on the very first run. One line.

Ruling: FINAL M1 folded in if it does not collide — ConfigTests has NO coverage of `birthday` or `dedication` validation, the two keys the milestone exists to deliver and the two least-tested in the file. Four assertions. Note ConfigTests.swift is one of the parallel session's files, so hunk discipline applies.

Ruling: FINAL M4 — NOT MINE TO FIX, surface to the user. README at 12d499f documents an `olive` coat as the default, but `CatPalette` on this branch has no olive (`all = [orange, grey, black, white, tuxedo, siamese]`, `default = orange`). Anyone following the README gets a "color=olive is out of range, using orange" correction. This is the two change sets colliding in COMMITTED history: the README text came from the parallel session's uncommitted work, preserved per R14. Reverting their prose is not mine to do, and landing their palette is not mine either. Tiago decides: land the olive work, or revert those README lines. — If wrong either way, one README paragraph is stale.

Triage accepted for AFTER hand-over: known items 1-9 and 11-13, plus FINAL M2 (replier's dwell not re-rolled, ~5% of meetings it walks off mid-reply), M3 (--settings-test can leave gift keys overwritten if it bails early), M5 (--shot renders only pet 0). Reviewer also corrected known item 7: `Pet.profile` IS mutated at AppDelegate.swift:1268 and must stay `var` — my deferred-minor list was wrong.

BIGGEST UNCOVERED SURFACE, per the reviewer: `considerMeeting()` has ZERO integration coverage. A regression that never calls it, faces both cats the same way, reverses one walker, or shows the reply on the opener would pass all five self-test modes and every check in GeometryTests. Recommend a `--meeting-test` after hand-over.

Ruling: Agent D's attribution of the --render-test failures was WRONG, and I verified it rather than accepting it — the second misattribution to the parallel session in this run. Method: `git archive` of committed HEAD and of the M10 baseline into scratchpad copies (archive reads history only and cannot touch the working tree or their uncommitted work), then built and ran each.
  - Clean committed HEAD (e293065, none of their WIP present): build warning-clean, GeometryTests 471/471, --menu-test 0, --interaction-test 0, --dedication-test 18/18 — and --render-test FAILS 14 of 44.
  - M10 baseline (8f4bdcc): --render-test FAILS 14 of 44, the SAME failures.
  Conclusion: M11 introduced ZERO --render-test regressions. The cause is that Task 0's `git add -A` baseline swept in the parallel session's MID-EDIT `makesprite.swift`, whose Ink values no longer match the committed `CatPalette.base` — the failing check says so outright ("found no pixels of #E8954A — makesprite.swift's Ink values have drifted from CatPalette.base"). The other 13 failures are the generated idle/sit/sleep sheets lacking distinct poses and not reaching the frame bottom. All inherited from the baseline, all belonging to their in-flight art work, none ours. It should resolve when they regenerate the art against the olive palette.
  D was directionally right (it is their work) but mechanically wrong (it is in COMMITTED history via the baseline, not in the working tree), and that distinction matters because it means the failure follows the branch anywhere it goes.

Agent D: complete (commit e293065). --dedication-test, 18 checks, guard preserved via a SEPARATE `isDedicationTest` flag rather than reusing isSelfTest, so the other four modes still cannot spend a real dedication. StateStore redirected by a nil-default `directoryOverride`. Teeth proven with three deliberate breaks, each reverted: skipping Phrasebook.render failed the two {name} checks; reintroducing the historical "dedication swallows the prompt's effect" bug failed exactly the nap check; dropping the lastGreetedDay write failed five stamp checks including "second click is not the dedication". Real state.json never created; real config.json has no dedication/birthday after all runs.

Ruling: the --render-test discrepancy is RECONCILED, and both measurements were correct. `Resources/sprites/` is GITIGNORED — the sprite art is not versioned. My scratch snapshots had no art, so `swift makesprite.swift` regenerated it from the COMMITTED makesprite.swift, which produces art inconsistent with the committed CatPalette.base -> 14 failures. The real working tree has art regenerated at 17:10 by the parallel session's MODIFIED makesprite.swift, which the same checks pass against -> 0 failures. Neither agent was lying; the variable is which art is on disk, and it is not in git.
  Consequence for hand-over: the gift is the built DockPet.app, which bundles whatever art existed at bundle time, so the gift itself is unaffected. But a fresh `git clone` + `./bundle.sh` on another machine regenerates art from committed makesprite.swift and lands in the failing state. That is a genuine repo-health issue belonging to the parallel session's in-flight work, not to M11, and it resolves when they commit a consistent makesprite/CatPalette pair. Surfaced to the user rather than fixed, since makesprite.swift is theirs and mid-edit.

Final fix wave: complete. Commits 530e16f (fix 1), cf3396b (fixes 2,3,6), 6aacfa7 (fixes 4,5,7 + README). GeometryTests 485 -> 493. Committed tree alone: --settings-test 46/46, --render-test 41/41, zero failures. Working tree shows 2 --settings-test failures, verified as the parallel session's uncommitted olive hunk.
  Fix 1 verified against the actual failure mode: support directory moved aside, before = exit 1 with "!! FATAL: no sprite sheets were loaded for 0 pet(s)", after = the app starts and stays up. The "before" was built from a scratch copy rather than rolling anything back in the checkout, which is the right way to do it here. Config directory restored and diff -r verified byte-identical twice.

Final fix wave re-review: ALL FINDINGS ADDRESSED, no new breakage. Verified one shared grant path (MenuBarItem and OnboardingWindow both terminate in AppDelegate.requestDockConfinement, no duplicated URL-open logic); the applyConfig clamp lands before `config = newConfig` so it persists; the bubble dismissal is synchronous inside the same closure so it cannot race the 1.5s delay; and every test hunk is purely additive with no assertion weakened.

Ruling: THE RED SUITE IS NOT OURS — established by direct measurement, not by any agent's word, after three agents gave three different numbers. Method: built the M10 baseline (8f4bdcc) and final HEAD in separate scratch copies with the SAME sprite art on disk.
  M10 baseline : --render-test 14/44 FAIL, --settings-test 3/22 FAIL
  M11 HEAD     : --render-test 14/44 FAIL (identical), --settings-test 4/46 FAIL
  The render failures are byte-identical between the two. The settings failures are the same three coat checks plus one more — the per-pet version of that same check, which M11 added and which fails for the same external reason. Every single failure message is a coat/palette pixel mismatch ("found 0 grey coat pixels", "found no pixels of #E8954A — makesprite.swift's Ink values have drifted from CatPalette.base"). ZERO failures are caused by M11.
  Root cause: the sprite art on disk no longer contains the base-palette pixels the recolour path looks for, so recolouring finds nothing to repaint. Both the committed makesprite.swift AND the working-tree art fail this way, which means the mid-edit generator went into the baseline commit via Task 0's `git add -A`. It resolves when the parallel session lands a matching generator/palette pair.
  USER-VISIBLE CONSEQUENCE worth stating plainly: until then, changing the coat setting does nothing visible. The cat renders fine; it just cannot be repainted.

Ruling: /Applications/DockPet.app rebuilt from COMMITTED HEAD rather than from the working tree, and the stale mid-refactor instance (PID 23192) killed and replaced. — Installing from the working tree would have shipped the parallel session's half-finished olive work into the user's installed app. Building from committed HEAD gives a coherent, fully reviewed binary. The sprite art was carried over from disk because it is gitignored and therefore absent from `git archive`. — Verified the new bundle carries the same designated requirement (certificate leaf H"26cde287…"), so the existing Accessibility grant survives the replacement. Now running as PID 68623. — If wrong: `./install.sh` rebuilds from the working tree whenever they want the olive work included.
